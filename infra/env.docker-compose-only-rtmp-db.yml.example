# ===================================================
# Docker Compose - RTMP + DB Only (Backend/Frontend Local)
# ===================================================
# Copy to .env: cp env.docker-compose-only-rtmp-db.yml.example .env
# ===================================================

# Database
POSTGRES_USER=gostream
POSTGRES_PASSWORD=gostream_secret_2024
POSTGRES_DB=gostream
POSTGRES_PORT=5432

# RTMP - Backend URL for webhooks
# IMPORTANT: Backend runs locally (not in Docker), RTMP is in Docker
# 
# ❌ DO NOT use: http://localhost:4000 (won't work - localhost = container itself)
# ❌ DO NOT use: http://backend:4000 (won't work - backend is not a Docker service)
# 
# ✅ USE ONE OF THESE:
#   Mac/Windows: http://host.docker.internal:4000
#   Linux: http://172.17.0.1:4000 (or find your Docker bridge IP: ip addr show docker0)
#   Linux alternative: http://YOUR_HOST_IP:4000 (your machine's IP address)
BACKEND_URL=http://host.docker.internal:4000
RTMP_PORT=1935
HLS_PORT=8080
