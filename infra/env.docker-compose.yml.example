# ===================================================
# Docker Compose - Full Stack (Backend + Frontend + RTMP + DB)
# ===================================================
# Copy to .env: cp env.docker-compose.yml.example .env
# ===================================================

# Database
POSTGRES_USER=gostream
POSTGRES_PASSWORD=gostream_secret_2024
POSTGRES_DB=gostream
POSTGRES_PORT=5432

# Backend
BACKEND_PORT=4000
JWT_SECRET=4f8a5c2e9b1d6f3a7e0c8b4a2d9f6e1c3b7a5d8f2e6c9b3a1f7d4e8c2b5a9f6e3
COOKIE_SECRET=4f8a5c2e9b1d6f3a7e0c8b4a2d9f6e1c3b7a5d8f2e6c9b3a1f7d4e8c2b5a9f6e3
NODE_ENV=production
CORS_ORIGIN=http://localhost:3000

# Frontend
FRONTEND_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_HLS_URL=http://localhost:8080/hls

# RTMP
RTMP_PORT=1935
HLS_PORT=8080

# Backend URL for RTMP webhooks
# Since both RTMP and Backend are in Docker (same network):
#   Use Docker service name: http://backend:4000
# If backend was local (not in Docker), use:
#   Mac/Windows: http://host.docker.internal:4000
#   Linux: http://172.17.0.1:4000 (or your host IP)
BACKEND_URL=http://backend:4000
